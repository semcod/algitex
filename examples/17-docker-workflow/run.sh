#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"
echo "=== Docker Workflow Example ==="
echo "This example demonstrates refactoring workflow with Docker tools."
echo "See README.md for workflow file and execution instructions."
