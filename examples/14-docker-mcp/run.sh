#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"
echo "=== Docker MCP Example ==="
echo "This example demonstrates Docker container management with docker-mcp."
echo "See README.md for CLI usage examples."
